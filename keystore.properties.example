# Copy this file to keystore.properties and fill in the private values locally.
# keystore.properties is ignored by Git. Never commit it or the keystore.
storeFile=/absolute/path/to/bard-release.jks
storePassword=REPLACE_LOCALLY
keyAlias=bard
keyPassword=REPLACE_LOCALLY
